+ get properties deleting
+ do a react binding example
+ remove the d3 binding one, unless we can make it much better
+ maybe do a prong example


+ experiment with more arbitrary modifications of the state - subchildren,
	adding, removing, moving objects around different levels??

+ do tests with grafting

+ throw errors when a watchable object is given more than one parent

+ start some documentation